<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OMY"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".locations.LocationSeeReviewActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".maps.MapAddActivity"
            android:exported="true" />
        <activity
            android:name=".maps.MapsCreatedActivity"
            android:exported="true" />
        <activity
            android:name=".trips.TripShowActivity"
            android:exported="true" />
        <activity
            android:name=".photos.PhotoShowActivity"
            android:exported="true" />
        <activity
            android:name=".locations.LocationShowActivity"
            android:exported="true" />
    </application>

</manifest>